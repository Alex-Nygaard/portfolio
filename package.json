{
  "name": "testportfolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\"",
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build",
    "prod": "cd client && npm install && npm run build && cd .. && node server/index.js",
    "dev": "cd client && npm install && npm start",
    "heroku-prebuild": "npm install && cd client && npm install"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.3",
    "file-saver": "^2.0.5",
    "react-icons": "^4.3.1"
  },
  "engines": {
    "node": "17.2.0"
  }
}
